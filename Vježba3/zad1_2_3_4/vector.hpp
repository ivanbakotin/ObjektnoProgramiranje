#include <iostream>
#include <vector>

using namespace std;

void inputVectorElements(vector<int> &vector, int n);

void inputVectorInterval(vector<int> &vector, int a, int b);

void printVector(vector<int> vector);

vector<int> firstNotInSecondVector(vector<int> first, vector<int> second);

void sortVector(vector<int> &vector);

void removeVectorElement(vector<int> &vector, int n);
